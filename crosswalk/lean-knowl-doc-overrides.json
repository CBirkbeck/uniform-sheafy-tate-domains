{
  "schema": 1,
  "commit": "b007a4f3d4226f00a684b402715aa542e2f0bcdc",
  "overrides": {
    "FiniteJet.GraphKoszul.koszulGraph_restricted_exact": "Let `E` be a complete nonarchimedean normed commutative ring whose norm unit ball is noetherian, and suppose that `E` has a topologically nilpotent unit which scales the norm multiplicatively. Put `T_E = E⟨T₁,…,T_m⟩` and `r_i = gT_i-f_i`, where `g,f₁,…,f_m` generate the unit ideal of `E`. Then, for every `q ≥ 0`, `im(d_{q+2,E}) = ker(d_{q+1,E})`. Thus every positive-degree Koszul cycle is a boundary.\n\nIn Lean, `P E m` is the ring denoted `T_E` in the paper. The theorem is obtained by transporting polynomial Koszul exactness along the flat map `E[T₁,…,T_m] → T_E`.",
    "FiniteJet.KoszulFree.koszulDifferential_continuous": "Give `∧^q T_E^m` the finite-product topology in the standard exterior basis. For every `q ≥ 0`, the Koszul differential `d_{q+1,E}: ∧^{q+1}T_E^m → ∧^qT_E^m` is continuous.\n\nLean proves this over any topological ring, since each coordinate is a finite sum of continuous multiplication maps.",
    "FiniteJet.GraphKoszul.koszulDifferential_isStrict": "For every `q ≥ 0`, the Koszul differential `d_{q+1,E}: ∧^{q+1}T_E^m → ∧^qT_E^m` is strict onto its image.\n\nIn the Gauss-norm/unit-ball case formalised in Lean, this follows from norm-controlled lifts from the image.",
    "FiniteJet.GraphKoszul.isClosed_range_koszulDifferential": "For every `q ≥ 0`, `im(d_{q+1,E})` is closed in `∧^qT_E^m`; when `q = 0`, this says that `I_E = (r₁,…,r_m)` is closed in `T_E`.\n\nLean uses positive-degree exactness and continuity when `q > 0`, and the noetherian closed-ideal theorem when `q = 0`.",
    "FiniteJet.GraphKoszul.pow_smul_graphIdeal_inter_unitBall_subset": "There is an integer `h_E ≥ 0` such that `ϖ^{h_E}(I_E ∩ T_{E,0}) ⊆ d_{1,E}(T_{E,0}^m)`.\n\nLean interprets the two lattices as the Gauss-norm and sup-norm unit balls. The result is proved for any finite sequence `r_i` by scaling a norm-controlled lift for `d_{1,E}`.",
    "FiniteJet.GraphKoszul.pow_smul_ker_d1_inter_subset": "There is an integer `z_E ≥ 0` such that `ϖ^{z_E}(ker(d_{1,E}) ∩ T_{E,0}^m) ⊆ d_{2,E}(∧²T_{E,0}^m)`.\n\nLean uses the standard finite-free coordinate model of `∧²T_E^m`, whose unit ball is `∧²T_{E,0}^m`, and obtains the exponent by scaling a norm-controlled lift for `d_{2,E}`.",
    "FiniteJetOver.StrictLoc.locRhoC_isOpenMap": "For every rational datum, the induced map `C_α → D_α` is open. Together with the separately proved continuity and surjectivity, this gives the strict surjection in Proposition 5.3.",
    "FiniteJetOver.chartDatum_isRational": "The datum `(W;ϖ)` defines the rational domain `{|W| ≤ |ϖ|}` used in Section 4.",
    "FiniteJetOver.chartEquiv_continuous": "The ring equivalence identifying the rational chart with `K⟨X,Q⟩/(Q²)` is continuous.",
    "FiniteJetOver.chartEquiv_symm_continuous": "The inverse of the ring equivalence identifying the rational chart with `K⟨X,Q⟩/(Q²)` is continuous. Together with `chartEquiv_continuous`, this makes the equivalence a homeomorphism.",
    "FiniteJetOver.locRhoC_continuous": "For every rational datum, the induced map `C_α → D_α` is continuous.",
    "FiniteJetOver.presheafValueMapB_restriction": "The coefficient map from `A` to `B` commutes with restriction between rational domains. This is the `B`-part of the refinement compatibility in Proposition 5.3.",
    "FiniteJetOver.presheafValueMapC_restriction": "The coefficient map from `A` to `C` commutes with restriction between rational domains. This is the `C`-part of the refinement compatibility in Proposition 5.3.",
    "ValuationSpectrum.IsSheafy": "Let `(A,A⁺)` be a complete nonarchimedean Huber pair. For every finite rational cover of a rational domain, restriction to the covering pieces is required to be a topological embedding, and every compatible family is required to glue. In the complete Tate setting this is the rational-basis form of the topological sheaf condition used in the paper.",
    "ValuationSpectrum.IsLimitSheaf": "For every open `V ⊆ Spa(A,A⁺)` and every open cover `V = ⋃ᵢ Uᵢ`, sections are separated, compatible families glue, and the restriction map `𝒪(V) → ∏ᵢ 𝒪(Uᵢ)` is a topological embedding. This is the all-open topological sheaf condition used in the paper.",
    "ValuationSpectrum.isSheafy_iff_isLimitSheaf": "For a complete Tate pair, the finite rational-cover condition `IsSheafy A` is equivalent to the all-open condition `IsLimitSheaf A`.",
    "ValuationSpectrum.IsSheafOfTopologicalRings": "For every topological commutative ring `T`, the presheaf `V ↦ Hom_cont(T,𝒪(V))` is a sheaf of sets. This is Wedhorn’s representable formulation of a sheaf of topological rings.",
    "ValuationSpectrum.isSheafOfTopologicalRings_iff_isLimitSheaf": "Wedhorn’s representable condition `IsSheafOfTopologicalRings A` is equivalent to separation, gluing and the topological-embedding condition for the all-open structure presheaf, namely `IsLimitSheaf A`.",
    "ValuationSpectrum.IsSheafyFor": "Let `A⁺` be a ring of integral elements of the complete Tate ring `A`. Then `IsSheafyFor A A⁺` means that the all-open structure presheaf on `Spa(A,A⁺)` satisfies `IsLimitSheaf`.",
    "ValuationSpectrum.IsSheafyComplete": "A complete Tate ring `A` satisfies `IsSheafyComplete` if `IsSheafyFor A A⁺` holds for every ring of integral elements `A⁺`.",
    "ValuationSpectrum.IsSheafyTateRing": "A Tate ring `A` satisfies `IsSheafyTateRing` if, for every pair of definition and every ring of integral elements of the resulting completion, the completed pair is sheafy. This is the choice-independent Tate-ring form of sheafiness used in the paper.",
    "ValuationSpectrum.isSheafyFor_congr": "Any two rings of integral elements in the same complete Tate ring give equivalent pair-level sheafiness conditions.",
    "ValuationSpectrum.isSheafyFor_iff_standardSheafCondition": "For a complete Tate ring, pair-level sheafiness for any ring of integral elements is equivalent to the plus-ring-independent standard rational-cover condition.",
    "ValuationSpectrum.isSheafyFor_iff_isSheafyComplete": "For a complete Tate ring, sheafiness for one ring of integral elements is equivalent to sheafiness for every ring of integral elements.",
    "ValuationSpectrum.isSheafyTateRing_iff_for_completionModel": "For any pair of definition `P`, the Tate ring `A` is sheafy if and only if the completion model determined by `P` is sheafy for every ring of integral elements.",
    "ValuationSpectrum.isSheafyTateRing_iff_isSheafyComplete": "If `A` is complete, the Tate-ring condition `IsSheafyTateRing A` is equivalent to `IsSheafyComplete A`; completion introduces no further choice.",
    "MvPowerSeries.IsRestricted": "A formal power series is restricted if its coefficients tend to zero: every neighbourhood of zero contains all but finitely many coefficients. These are the elements of `A⟨T₁,…,Tₖ⟩`.",
    "restrictedMvPowerSeriesSubring": "`restrictedMvPowerSeriesSubring k A` is the subring of `A⦃T₁,…,Tₖ⦄` consisting of restricted series; mathematically it is `A⟨T₁,…,Tₖ⟩`.",
    "IsStronglyNoetherian": "A topological ring `A` is strongly noetherian when `A⟨T₁,…,Tₖ⟩` is noetherian for every `k ≥ 0`. For complete `A`, this is the condition appearing in Wedhorn’s definition.",
    "FiniteJetOver.L": "`L K` is the radius-one Laurent algebra `K⟨W,W⁻¹⟩`.",
    "FiniteJetOver.JetB": "`JetB K` is `𝓑 = K⟨W,Q⟩/(Q²)`, represented in Lean as the dual numbers over `K⟨W⟩`.",
    "FiniteJetOver.JetC": "`JetC K` is `𝓒 = K⟨W,W⁻¹⟩⟨Q⟩`.",
    "FiniteJetOver.JetD": "`JetD K` is `𝓓 = K⟨W,W⁻¹⟩⟨Q⟩/(Q²)`, represented in Lean as the dual numbers over `K⟨W,W⁻¹⟩`.",
    "FiniteJetOver.jetSupport": "Inside `𝓒 = K⟨W,W⁻¹⟩⟨Q⟩`, this is the subring of series `∑ₙ aₙQⁿ` for which `a₀,a₁ ∈ K⟨W⟩`; no condition is imposed on `aₙ` for `n ≥ 2`. Equivalently, its elements have the form `a₀ + Qa₁ + Q²h` with `h ∈ K⟨W,W⁻¹⟩⟨Q⟩`.",
    "FiniteJetOver.JetA": "`JetA K` is the preceding subring of `𝓒`, with the norm and topology inherited from `𝓒`; it is the Lean model of the paper’s ring `A`.",
    "FiniteJetOver.milnorRow_exact": "For every pair `(b,c) ∈ 𝓑 × 𝓒` with the same image in `𝓓`, there is a unique `a ∈ 𝓐` mapping to `(b,c)`. Hence `𝓐 ≅ 𝓑 ×_𝓓 𝓒`.",
    "FiniteJetOver.isTateRing_JetA": "With a chosen uniformizer of `K`, the finite-jet algebra `𝓐` is a Tate ring.",
    "FiniteJetOver.isPowerBounded_JetA_iff": "An element of `𝓐` is power-bounded if and only if its norm is at most one. Thus `𝓐°` is precisely the norm unit ball, which is the paper’s `A₀`.",
    "FiniteJetOver.finiteJet_isDomain": "The finite-jet algebra `𝓐` is an integral domain.",
    "FiniteJetOver.finiteJet_not_noetherian": "The finite-jet algebra `𝓐` is not noetherian.",
    "FiniteJetOver.finiteJet_isUniform_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then `𝓐` is uniform.",
    "FiniteJetOver.finiteJet_not_stablyUniform_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then `𝓐` is not stably uniform.",
    "FiniteJetOver.isSheafy_JetA": "Choose a uniformizer of `K` and suppose that the unit ball of `K` is noetherian. Then the maximal pair `(𝓐,𝓐°)` satisfies the finite rational-cover condition `IsSheafy`. This is the condition proved directly by the Milnor-square argument.",
    "FiniteJetOver.isSheafy_JetA_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then the maximal pair `(𝓐,𝓐°)` satisfies the finite rational-cover condition `IsSheafy`.",
    "FiniteJetOver.finiteJet_isSheafyFor": "Choose a uniformizer of `K` and suppose that the unit ball of `K` is noetherian. Then the all-open structure presheaf on `Spa(𝓐,𝓐°)` is a sheaf of topological rings.",
    "FiniteJetOver.finiteJet_isSheafyComplete": "Choose a uniformizer of `K` and suppose that the unit ball of `K` is noetherian. Then `(𝓐,A⁺)` is sheafy for every ring of integral elements `A⁺` of `𝓐`.",
    "FiniteJetOver.finiteJet_isSheafyTateRing": "Choose a uniformizer of `K` and suppose that the unit ball of `K` is noetherian. Then every completion model of `𝓐` is sheafy for every ring of integral elements.",
    "FiniteJetOver.finiteJet_isSheafyFor_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then the all-open structure presheaf on `Spa(𝓐,𝓐°)` is a sheaf of topological rings.",
    "FiniteJetOver.finiteJet_isSheafyComplete_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then `(𝓐,A⁺)` is sheafy for every ring of integral elements `A⁺` of `𝓐`.",
    "FiniteJetOver.finiteJet_isSheafyTateRing_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then every completion model of `𝓐` is sheafy for every ring of integral elements.",
    "FiniteJetOver.finiteJet_structurePresheaf_isSheafOfTopologicalRings_all_of_dvr": "If the valuation ring of `K` is a discrete valuation ring and `A⁺` is any ring of integral elements of `𝓐`, then the all-open structure presheaf on `Spa(𝓐,A⁺)` is a sheaf of topological rings.",
    "FiniteJetOver.chartEquiv": "There is a ring equivalence between the rational chart `𝒪_𝓐({|W| ≤ |ϖ|})` and `K⟨X,Q⟩/(Q²)`. The two companion continuity theorems show that it is a homeomorphism.",
    "FiniteJetOver.chartEquiv_canonicalMap_W": "Under the chart equivalence, the canonical generators satisfy `W ↦ ϖX` and `Q ↦ Q`, as in Proposition 4.1.",
    "FiniteJetOver.not_isUniform_chart": "The rational chart `𝒪_𝓐({|W| ≤ |ϖ|})` is not uniform.",
    "FiniteJetOver.StrictLoc.ext_pair_injective": "For the relation ideals `I_E ⊂ T_E`, the canonical map `I_A → I_B ×_{I_D} I_C` is injective.",
    "FiniteJetOver.StrictLoc.ideal_row_surjective": "The map `I_B ⊕ I_C → I_D` is surjective. Its proof also gives a uniform bounded-denominator lift, which is the right-hand strictness assertion in Lemma 5.2.",
    "FiniteJetOver.StrictLoc.ideal_pullback_controlled": "There is a fixed exponent `N` such that every integral compatible pair in `I_B ×_{I_D} I_C`, after multiplication by `ϖ^N`, lifts from the integral lattice in `I_A`. This is the bounded-denominator estimate used for strictness in Lemma 5.2.",
    "FiniteJetOver.StrictLoc.isClosed_IA": "The relation ideal `I_A = (gT_i-f_i)_i` is closed in `T_A`.",
    "FiniteJetOver.StrictLoc.loc_row_exact": "For every rational datum, the sequence `0 → A_α → B_α ⊕ C_α → D_α → 0` is algebraically exact.",
    "FiniteJetOver.StrictLoc.loc_pair_isEmbedding": "For every rational datum, the canonical map `A_α → B_α ×_{D_α} C_α` is a topological embedding. Together with algebraic exactness, this identifies `A_α` with the topological fibre product.",
    "FiniteJetOver.StrictLoc.locRhoC_surjective": "For every rational datum, the induced map `C_α → D_α` is surjective.",
    "WeightedParity.wpWeight": "For a finitely supported exponent vector `ν`, `wpWeight w ν` is the sum of `w(n)` over the nonzero indices at which `νₙ` is odd. Index zero is reserved for the variable `W` and does not contribute.",
    "WeightedParity.WPMem": "An exponent vector belongs to the weighted-parity support precisely when its exponent of `W` is at least its parity weight. For `w(n)=n`, this is the paper’s monoid `S = {(a,ν) : a ≥ ω(ν)}`.",
    "WeightedParity.wpSupport": "Inside the countable restricted Tate algebra `K⟨W,U₁,U₂,…⟩`, `wpSupport K w` consists of the series whose coefficients vanish outside the weighted-parity support monoid.",
    "WeightedParity.WPA": "`WPA K w` is the weighted-parity support subring with the norm and topology inherited from the countable restricted Tate algebra.",
    "WeightedParity.constA": "`constA` is the constant-series embedding `K → 𝒜`.",
    "WeightedParity.norm_constA": "The constant-series embedding `K → 𝒜` preserves the norm.",
    "WeightedParity.idWeight": "`idWeight(n)=n` is the weight used in the paper.",
    "WeightedParity.WPAid": "`WPAid K` is `WPA K idWeight`; it is the Lean model of the paper’s weighted-parity algebra `𝒜`.",
    "WeightedParity.isTateRing_WPA": "A chosen uniformizer of `K` gives `WPA K w` a topologically nilpotent unit, and hence makes it a Tate ring.",
    "WeightedParity.weightedParity_isUniform_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then the weighted-parity algebra `𝒜` is uniform.",
    "WeightedParity.weightedParity_isDomain": "The weighted-parity algebra `𝒜` is an integral domain.",
    "WeightedParity.weightedParity_powerBounded_eq_unitBall": "The power-bounded elements of `𝒜` are exactly its norm unit ball. This is the ring denoted `𝒜₀` in the paper.",
    "WeightedParity.weightedParity_not_noetherian": "The weighted-parity algebra `𝒜` is not noetherian.",
    "WeightedParity.weightedParity_isSheafyComplete_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then `(𝒜,A⁺)` is sheafy for every ring of integral elements `A⁺`. In particular, the pair `(𝒜,𝒜°)` in the paper is sheafy.",
    "WeightedParity.wp_structurePresheaf_isSheaf_all": "For any valid ring of integral elements `A⁺`, the all-open structure presheaf on `Spa(𝒜,A⁺)` is a sheaf. This is the direct all-open form of the weighted-parity sheaf theorem.",
    "WeightedParity.chartDatum": "This is the rational datum `(W;ϖ)` on `𝒜`, defining the chart `{|W| ≤ |ϖ|}`.",
    "WeightedParity.chartDatum_isRational": "The datum `(W;ϖ)` satisfies the unit-ideal condition required of a rational localisation datum.",
    "WeightedParity.weightedParity_chart_isDomain": "The section ring on the rational chart `{|W| ≤ |ϖ|}` is an integral domain.",
    "WeightedParity.weightedParity_chart_not_isUniform": "The section ring on the rational chart `{|W| ≤ |ϖ|}` is not uniform.",
    "WeightedParity.weightedParity_not_stablyUniform_of_dvr": "If the valuation ring of `K` is a discrete valuation ring, then `𝒜` is not stably uniform."
  }
}
