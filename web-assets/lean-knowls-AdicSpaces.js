// No doc-gen4 bundle is published for this AINTLIB snapshot yet.  Defining an
// empty registry makes Paperforge render its automatic declaration badges as
// honest inert pills; the appendix contains the pinned source links.
window.PAPERFORGE_LEAN_KNOWLS = Object.assign(
  window.PAPERFORGE_LEAN_KNOWLS || {},
  {}
);
